{
  "name": "petos",
  "version": "0.1.0-alpha.0",
  "private": true,
  "description": "A living-creature simulation engine for desktop pets.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test tests/*.test.mjs",
    "demo": "npm run build && node dist/demo.js"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
